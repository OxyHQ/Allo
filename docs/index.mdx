---
title: Allo
description: End-to-end encrypted, device-first chat app for iOS, Android, and Web.
order: 0
---

# Allo

A modern, cross-platform chat app built with Expo, React Native, and a Node.js / Express backend. Direct messages are end-to-end encrypted with static ECDH (P-256) + AES-256-GCM, stored on the device first, and relayed through the server as opaque ciphertext when cloud sync is on.

<Badge variant="success">App</Badge> <Badge>iOS / Android / Web</Badge>

## What it is

Allo is the Oxy ecosystem's encrypted messenger. The frontend is an Expo Router app that targets iOS, Android, and Web from one codebase. The backend is a stateless Express service that brokers conversations, device keys, and Socket.IO traffic — it never holds plaintext.

Identity is delegated to the Oxy platform: users sign in with their Oxy account through `@oxyhq/services`, the backend trusts the resulting JWT, and there is no separate user database in Allo. The only per-user records the server holds are conversation membership, device keys, encrypted message blobs, and a small `UserSettings` document.

For account management UI see [MyAccount](/developers/docs/accounts). For the underlying RN SDK see [@oxyhq/services](/developers/docs/services).

**Migrating to Matrix.** Allo will stop carrying its own transport; the design
work is under [Matrix](#documentation-map) at the bottom of this page and
deliberately precedes any implementation. Everything described here is still
what the app actually does today.

## Architecture

```
   ┌──────────────────────┐                  ┌──────────────────────┐
   │   Allo Frontend      │  ── REST ─────▶  │   Allo Backend       │
   │   (Expo / RN / Web)  │                  │   (Express + Mongo)  │
   │                      │  ◀── Socket.IO ─ │                      │
   └──────────────────────┘                  └──────────────────────┘
            │                                          │
            ▼                                          ▼
     SecureStore / AsyncStorage              MongoDB (ciphertext only)
     (device keys, message cache,            +  Oxy API (auth, profiles)
      offline queue)
```

The split is strict: every byte the server stores for a `Message` document is opaque to it. Plaintext only exists on the sender's device, on the recipient's device, and in transit over TLS to the API.

Inside `packages/`:

| Package | Role |
|---------|------|
| `frontend` (`@allo/frontend`) | Expo + Expo Router app. NativeWind for styling, Zustand for local state, TanStack-style stores around the encrypted message cache. |
| `backend` (`@allo/backend`) | Express + Socket.IO. Mongoose models for conversations, messages, devices, settings, blocks, restricts, push tokens. Auth via `@oxyhq/core`'s `oxy.auth()`. |
| `shared-types` (`@allo/shared-types`) | TypeScript-only package built ahead of the others (a `postinstall` script runs `build:shared-types`). |

Build order matters: `shared-types` -> `backend` + `frontend` (in parallel).

## Threat model

- **What the server sees:** ciphertext (when encryption succeeds), message size, sender/recipient envelope (conversation id, sender Oxy user id, sender device id), and timing.
- **What the server cannot see, for successfully encrypted messages:** message bodies. There is currently no working encrypted-media path — see [Encryption](./encryption).
- **What a stolen device cannot do:** read messages without the OS-level credential that gates `expo-secure-store` (Face ID / fingerprint / passcode). Private keys live in SecureStore on iOS / Android and never leave the device.
- **Recovery:** there is no server-side reset. Messages stored only on-device are gone if the device is lost without cloud sync enabled.
- **Important caveats:** encryption has no forward secrecy (a leaked identity key decrypts that pair's entire message history), group conversations with more than two participants aren't correctly encrypted for every member, a second device belonging to the same user generally cannot decrypt messages sent to that user, and the client falls back to sending plaintext when it can't encrypt rather than blocking the send. See [Encryption](./encryption) for the full picture.

Cloud sync is **opt-in** per user in `Settings -> Security & Encryption`. Until they enable it, the server's `Message` collection is the only off-device copy — and even that is ciphertext.

## Install

Clone the repo and install with bun:

```sh
git clone https://github.com/OxyHQ/Allo.git
cd Allo
bun install
```

`bun install` resolves the three workspaces, then `postinstall` builds `@allo/shared-types` so the other packages can type-check immediately.

### Prerequisites

- Node.js 20.19+ and Bun 1.3+
- MongoDB (local or hosted) — connection string goes into `packages/backend/.env`
- For iOS: macOS + Xcode. For Android: Android Studio.

### Environment

`packages/backend/.env`:

```env
MONGODB_URI=mongodb://localhost:27017/allo
# Read by @oxyhq/core, not by this package. Defaults to https://api.oxy.so.
OXY_API_URL=https://api.oxy.so
# 4140 is Allo's slot in the Oxy per-app port map; ECS injects PORT=8080.
PORT=4140
NODE_ENV=development
```

There is no `FRONTEND_URL` and no `JWT_SECRET`. Both appear in older deployment
docs and are read by neither this package nor `@oxyhq/core`; the CORS allowlist
is a literal in `server.ts` plus whatever `createOxyCors` admits automatically.

`packages/frontend/.env` — optional. `config.ts` hardcodes production and
defaults development to `localhost:4140`, so a local run needs nothing set:

```env
API_URL=http://localhost:4140/api
API_URL_SOCKET=ws://localhost:4140
EXPO_PUBLIC_OXY_CLIENT_ID=your_oxy_client_id
```

See the [frontend README](https://github.com/OxyHQ/Allo/blob/main/packages/frontend/README.md#environment-setup)
for the full list. `EXPO_PUBLIC_WS_URL` and `EXPO_PUBLIC_POSTHOG_KEY` were
documented here previously and are read nowhere.

## Quick start

```sh
# Start everything (frontend + backend)
bun run dev

# Or one at a time
bun run dev:backend
bun run dev:frontend
```

Then in the Expo CLI, press `w` for web, `i` for iOS simulator, or `a` for Android emulator.

## Core concepts

### Device-first storage

Allo treats the device as the source of truth. When a user sends a message:

1. The client encrypts the plaintext locally using its device keys (or falls back to plaintext if it can't — see [Encryption](./encryption)).
2. The ciphertext is written to AsyncStorage via `lib/offlineStorage.ts` and added to the conversation's Zustand `messagesStore`.
3. The encrypted payload is POSTed to `/api/messages`. If the network is unavailable, the mutation sits in the offline queue (`lib/offlineQueue/`) and replays on reconnect.
4. The recipient's device pulls or receives the ciphertext, decrypts locally, and renders.

The UI never blocks on the network — sent messages appear immediately and reconcile when the server responds.

### Device keys

Each install generates its own key bundle on first launch (`lib/signalProtocol.ts`, `stores/deviceKeysStore.ts`). The module is named `signalProtocol.ts` for historical reasons, but it does not implement the Signal Protocol — there's no X3DH, no Double Ratchet, and no per-session key derivation:

- **Identity key pair** — P-256, long-lived. The only key pair actually used to derive an encryption key.
- **Signed pre-key** — P-256, signed by the identity key with ECDSA over SHA-256. Generated and uploaded, but not used by any encrypt/decrypt path, and its signature is never verified.
- **One-time pre-keys** — 100 P-256 key pairs, uploaded to the backend but never consumed during encryption.
- **Registration id** — random 31-bit integer, persisted in `expo-secure-store`.

Public keys are published to `POST /api/devices`. Private keys never leave the device. Encryption derives a static ECDH shared secret between the sender's and recipient's identity keys (no KDF, so the key never changes for a given pair), then AES-256-GCM with a fresh 96-bit IV per message.

See [Encryption](./encryption) for the full picture, including what isn't implemented yet (forward secrecy, multi-device, correct group encryption, P2P, encrypted media).

### Conversations and messages

A `Conversation` is either `direct` (two participants) or `group` (named, with admins). The server-side schema lives in `packages/backend/src/models/Conversation.ts`:

- `participants[]` — `{ userId, role, joinedAt, lastReadAt }`
- `unreadCounts` — `Record<userId, number>` updated when a participant marks read
- `archivedBy` — per-user soft archive
- `lastMessage` — denormalized summary of the most recent message for list views

`Message` documents (`models/Message.ts`) store `ciphertext`, `senderDeviceId`, `encryptionVersion`, optional `encryptedMedia[]`, plus `readBy`, `deliveredTo`, and a soft-delete `deletedAt`.

### Real-time over Socket.IO

The backend exposes a `/messaging` namespace (`server.ts`). The client authenticates with the same Oxy bearer token and joins:

- `user:<userId>` — personal room for delivery / device events
- `conversation:<conversationId>` — per-conversation room for `newMessage`, `messageUpdated`, `messageDeleted`, and `typing` indicators

`joinConversation` / `leaveConversation` events scope subscriptions when the active thread changes. Typing indicators are broadcast to peers in the conversation room except the sender.

### Peer-to-peer (not yet functional)

`lib/p2pMessaging.ts` scaffolds a WebRTC data channel signaled over Socket.IO, but the connection-establishment path is a stub: it always reports failure, and the offer/answer/ICE-candidate handlers are unimplemented. Every message today goes through the REST relay when cloud sync is enabled.

### Routing

Frontend routing follows Expo Router 57 conventions:

```
app/
  _layout.tsx
  (auth)/
    index.tsx            # sign-in
  (chat)/
    _layout.tsx          # two-pane at >=768px, plus a details column at >=1024px
    index.tsx            # conversation list
    new.tsx              # start a conversation
    c/[id].tsx           # individual conversation
    u/[id].tsx           # user profile
    settings/...
  calls.tsx              # calls screen (mock data)
```

Mobile gets a single-pane stack. Tablet gets list + thread. Desktop adds a contact-details column. All three come out of the single `(chat)/_layout.tsx`.

## Documentation map

The system as it runs today:

- [Architecture](./architecture) — package layout, dependency graph, build order
- [Encryption](./encryption) — key bundle, static ECDH derivation, AES-GCM framing, and known gaps
- [API](./api) — backend REST and Socket.IO surface

Where it is going — design only, nothing implemented:

- [Matrix: data model](./matrix/data-model) — how Allo's conversations map onto Matrix rooms, and what that mapping costs
- [Matrix: bridges](./matrix/bridges) — bridge topology and its constraints
- [Matrix: client strategy](./matrix/client-strategy) — which SDK on which platform, and why
